## Node.js介绍

JavaScript 是语言，Node.js 是让这个语言在服务器端运行的环境，npm(Node Package Mananger)是主流管理Node.js软件包的内置工具

## 配置文件

```
package.json -核心配置包括：项目原数据(名称、版本、描述等),依赖管理(dependencies,devDependencies)，
脚本命令(scripts,执行npm run dev),入口文件配置，其他npm特定配置

Dockerfile -Docker镜像配置    .env -环境配置,等等
```